name: Legacy Postiz Analytics - Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Postiz analytics is disabled. Direct official-API analytics will replace this workflow.'
