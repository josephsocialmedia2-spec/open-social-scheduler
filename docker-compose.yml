version: "3.9"

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: scheduler
      POSTGRES_USER: scheduler
      POSTGRES_PASSWORD: scheduler
    ports:
      - "5432:5432"

  redis:
    image: redis:7
    ports:
      - "6379:6379"
