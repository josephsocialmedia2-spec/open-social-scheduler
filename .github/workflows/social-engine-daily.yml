name: Open Social Engine - MAINTENANCE

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  maintenance:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Publication engine temporarily paused while the new 5 F1 + 5 RMP policy is installed."
