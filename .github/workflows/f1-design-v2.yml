name: F1 Design V2 (ARCHIVED - ChatGPT Creative replaces final graphics)
on:
  workflow_dispatch:
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ARCHIVED. Final F1 creative production now uses .github/workflows/f1-chatgpt-creative.yml"
